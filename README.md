# upi-qr-code-generator

I was just messing around a bit with the upi so i write a few lines of code using the qrcode python library to generate a qr code with amount pre filled.
It is not a project to show I just put it in github. If in future I want to do some lil experiment with it rn I am thinking of doing the "2000rs split multi qr generator thing"